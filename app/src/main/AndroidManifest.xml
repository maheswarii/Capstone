<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yps.layani">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Layani">
        <activity android:name=".user.ui.home.CreateComplaintActivity"></activity>
        <activity android:name=".admin.ui.home.CreateComplaintActivity" />
        <activity
            android:name=".user.UserActivity"
            android:label="@string/title_activity_user" />
        <activity android:name=".user.HomeUserActivity" />
        <activity android:name=".admin.ui.detail.DetailComplaintActivity" />
        <activity
            android:name=".admin.HomeAdminActivity"
            android:label="@string/title_activity_home" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".StartActivity" />

        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </application>

</manifest>